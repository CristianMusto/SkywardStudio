import { skyward } from './skyward';
import { yourProject } from './your-project';
import type { PlanetData } from '../types';

// Order = order of the planets in the system and in the page navigation.
export const work: PlanetData[] = [skyward, yourProject];
